[package]
name = "jhlabs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
